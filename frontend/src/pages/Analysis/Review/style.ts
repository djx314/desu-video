import { css } from '@emotion/core'
import styled from '@emotion/styled'

export const TitleStyle = css({
  textAlign: "center",
})

export const FormStyle = css({
  margin: "30px 0",
})

export const GStyle = styled.div`
  width: 700px;
  max-width: 80%;
  margin: 0 auto;

  .ant-form-item {
    margin-bottom: 5px;
  }
`

export const LastBtnStyle = styled.div`
  .ant-btn {
    margin: 0 auto;
  }
`